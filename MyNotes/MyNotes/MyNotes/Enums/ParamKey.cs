namespace MyNotes.Enums
{
    public class ParamKey
    {
        public const string SheetId = nameof(SheetId);
        public const string ItemId = nameof(ItemId);
        public const string Title = nameof(Title);
    }
}
